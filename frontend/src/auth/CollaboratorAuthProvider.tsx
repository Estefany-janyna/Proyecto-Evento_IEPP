import {createContext,useContext,useMemo,useState} from 'react';type AuthValue={token:string|null;login:(token:string)=>void;logout:()=>void};const C=createContext<AuthValue|null>(null);export function CollaboratorAuthProvider({children}:{children:React.ReactNode}){const [token,setToken]=useState(localStorage.getItem('token'));const value=useMemo(()=>({token,login:(t:string)=>{localStorage.setItem('token',t);setToken(t)},logout:()=>{localStorage.removeItem('token');setToken(null)}}),[token]);return <C.Provider value={value}>{children}</C.Provider>}export function useCollaboratorAuth(){const v=useContext(C);if(!v)throw new Error('Provider requerido');return v}
