import {useEffect,useState} from 'react';import {publicService} from '../services';export function useDniLookup(dni:string){const [data,setData]=useState<any>(null),[loading,setLoading]=useState(false),[error,setError]=useState('');useEffect(()=>{if(!/^\d{8}$/.test(dni)){setData(null);setError('');return}const t=setTimeout(async()=>{try{setLoading(true);setData(await publicService.dni(dni));}catch{setError('No se pudo consultar el DNI')}finally{setLoading(false)}},400);return()=>clearTimeout(t)},[dni]);return{data,loading,error}}
