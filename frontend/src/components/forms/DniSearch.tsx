import {Input} from '../ui/Input';export function DniSearch({value,onChange,loading=false}:{value:string;onChange:(v:string)=>void;loading?:boolean}){return <div className="relative"><Input value={value} onChange={e=>onChange(e.target.value.replace(/\D/g,'').slice(0,8))} maxLength={8} inputMode="numeric" placeholder="Ingrese DNI"/><span className="absolute right-3 top-3 text-sm text-slate-500">{loading?'Consultando...':`${value.length}/8`}</span></div>}
